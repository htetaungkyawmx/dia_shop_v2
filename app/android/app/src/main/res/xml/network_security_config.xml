<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic must be HTTPS: the default below enforces it.
  The exception list only covers the addresses a development machine uses,
  so testing against a local API works without weakening the real app.
    10.0.2.2   - the host machine, as seen from the Android emulator
    localhost  - a device with adb reverse, or a desktop build
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Your Mac on the office wifi, for testing on a real phone.
             Change or remove this once you are pointing at your server. -->
        <domain includeSubdomains="false">192.168.0.230</domain>
    </domain-config>
</network-security-config>

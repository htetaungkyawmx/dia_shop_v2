# Copy to scripts/deploy.env and fill in. This file is gitignored.

# SSH target for your VPS.
VPS_HOST=root@203.0.113.10

# Your domain, without https:// and without a subdomain.
DOMAIN=example.com

# Where the repo lives on the VPS.
REMOTE_DIR=/opt/dia-shop
